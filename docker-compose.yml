services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # No ports mapping - container will only be accessible internally
    networks:
      - app_network

networks:
  app_network:
    driver: bridge